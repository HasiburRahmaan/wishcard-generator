import WishCardEditor from '@/components/pages/editor'

type Props = {}

const page = (props: Props) => {
  return (
    <main className="flex min-h-screen w-full">
      <WishCardEditor />
    </main>
  )
}

export default page